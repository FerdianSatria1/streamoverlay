version: "3.8"

services:
  streampulse:
    image: nginx:alpine
    container_name: streampulse-overlay
    restart: unless-stopped
    ports:
      - "11111:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./metadata.json:/usr/share/nginx/html/metadata.json:ro
